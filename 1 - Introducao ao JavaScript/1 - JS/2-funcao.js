// Declarando uma função.

function soma(a, b) {
    return a + b;
}

// Uso do console.log é para visualizar o resultado no console.
function soma(a, b) {
    console.log (a + b);
    return a + b;
}

// Chamando uma função.
soma(3, 5)